name: Neural Drop Daily Pipeline

on:
  schedule:
    # 4:00 AM Central Time = 09:00 UTC (CDT/UTC-5)
    - cron: "0 9 * * *"
  workflow_dispatch:
    inputs:
      count:
        description: "Number of videos to produce"
        default: "2"
      skip_buffer:
        description: "Skip Buffer scheduling (dry run)"
        type: boolean
        default: false
      skip_tts:
        description: "Skip TTS (test mode)"
        type: boolean
        default: false

jobs:
  daily-pipeline:
    runs-on: ubuntu-latest
    timeout-minutes: 90

    steps:
      # ── Checkout ────────────────────────────────────────────────────────────
      - name: Checkout repo
        uses: actions/checkout@v4

      # ── System deps ─────────────────────────────────────────────────────────
      - name: Install system dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y ffmpeg python3-dev libgl1 libglib2.0-0 \
            fonts-liberation fonts-open-sans

      # ── Python ──────────────────────────────────────────────────────────────
      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install Python dependencies
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt
          pip install opencv-python-headless webvtt-py

      # ── Montserrat font ──────────────────────────────────────────────────────
      - name: Install Montserrat font
        run: |
          mkdir -p assets/fonts/Montserrat
          wget -q "https://github.com/JulietaUla/Montserrat/raw/master/fonts/ttf/Montserrat-Bold.ttf" \
            -O assets/fonts/Montserrat/Montserrat-Bold.ttf
          wget -q "https://github.com/JulietaUla/Montserrat/raw/master/fonts/ttf/Montserrat-Black.ttf" \
            -O assets/fonts/Montserrat/Montserrat-Black.ttf
          wget -q "https://github.com/JulietaUla/Montserrat/raw/master/fonts/ttf/Montserrat-SemiBold.ttf" \
            -O assets/fonts/Montserrat/Montserrat-SemiBold.ttf

      # ── Run pipeline ────────────────────────────────────────────────────────
      - name: Run Neural Drop pipeline
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          NEWSDATA_API_KEY: ${{ secrets.NEWSDATA_API_KEY }}
          YOUTUBE_API_KEY: ${{ secrets.YOUTUBE_API_KEY }}
          PIXABAY_API_KEY: ${{ secrets.PIXABAY_API_KEY }}
          BUFFER_API_KEY: ${{ secrets.BUFFER_API_KEY }}
          BUFFER_CHANNEL_IDS: ${{ secrets.BUFFER_CHANNEL_IDS }}
          ZERNIO_API_KEY: ${{ secrets.ZERNIO_API_KEY }}
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
          GITHUB_STORAGE_REPO: ${{ secrets.GITHUB_STORAGE_REPO }}
          GITHUB_STORAGE_BRANCH: main
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          COUNT=${{ github.event.inputs.count || '2' }}
          SKIP_BUFFER_FLAG=""
          if [ "${{ github.event.inputs.skip_buffer }}" = "true" ]; then
            SKIP_BUFFER_FLAG="--skip-buffer"
          fi
          python3 run_daily.py --count $COUNT $SKIP_BUFFER_FLAG

      # ── Upload output artifacts ──────────────────────────────────────────────
      - name: Upload video artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: neural-drop-videos-${{ github.run_number }}
          path: output/*/video.mp4
          retention-days: 7
